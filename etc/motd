Welcome to Helium __VERSION__ (__CODENAME__)!

You can install packages from the FreeBSD package collection
using `pkg` (e.g. `pkg install vim`)

Although Helium is mostly compatible with FreeBSD, please
do not report issues to or bother the FreeBSD developers!
Report all issues at https://github.com/mszoek/helium/issues

Edit /etc/motd to change this login announcement.
