# Use this file to set your local environment to run the MallocBench tests,
# overriding MALLOC_BENCH_DIR, BMALLOC_LIB_DIR and SCRIPTS_DIR as required.
# Do not check in updates to this file.

# Choose ONE_RUN=--one_run to disable warmup and run each test only once.
#export ONE_RUN=--one_run
#export ONE_RUN=
#export MALLOC_BENCH_DIR=
#export BMALLOC_LIB_DIR=
