# Ignore devices created by libinput's test suite (litest)
Section "InputClass"
        Identifier "Ignore libinput test suite devices"
        MatchProduct "litest"
        Option "Ignore" "on"
EndSection
