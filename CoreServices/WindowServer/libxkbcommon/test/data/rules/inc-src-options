! option        = compat
  option111     = +substring
  option1       = +some:compat
  option11      = +group(bla)

! include %S/inc-dst-simple

! option        = symbols
  option3       = +compose(foo)+keypad(bar)
  colon:opt     = +altwin(menu)
