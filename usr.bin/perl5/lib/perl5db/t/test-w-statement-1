#!/usr/bin/perl

use strict;
use warnings;

use vars qw($foo);

$foo = 1;

print "Hello\n";

for my $idx (map { $_ * 10 } 1 .. 10)
{
    if ($idx > 17)
    {
        $foo = 2;
        print "Baz\n";
    }
}

